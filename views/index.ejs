<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Async Tasks Dashboard</title>
  <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
  />
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="icon" href="/Async_task_node.js/public/icons/images.jpeg" type="image/jpeg" />

</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4 text-primary">Asynchronous Task Submission</h1>

    <% const flashes = Array.isArray(flash) ? flash : []; %>
    <% if (flashes.length) { %>
      <% flashes.forEach(f => { %>
        <div class="alert alert-<%= f.type %>"><%= f.msg %></div>
      <% }) %>
    <% } %>

    <div class="row">
      <!-- Send Email -->
      <div class="col-md-6 mb-4">
        <div class="task-card email-task p-3">
          <div class="task-header">📧 Send Email</div>
          <form method="POST">
            <input type="hidden" name="form_type" value="send_email_task" />
            <input
              type="email"
              name="recipient"
              class="form-control mb-2"
              placeholder="Recipient Email"
              required
            />
            <input
              type="text"
              name="subject"
              class="form-control mb-2"
              placeholder="Subject"
            />
            <textarea
              name="body"
              class="form-control mb-2"
              placeholder="Email Body"
            ></textarea>
            <button class="btn btn-light w-100" type="submit">
              Send Email
            </button>
          </form>
        </div>
      </div>

      <!-- Reverse Text -->
      <div class="col-md-6 mb-4">
        <div class="task-card reverse-task p-3">
          <div class="task-header">📈 Reverse Text</div>
          <form method="POST">
            <input type="hidden" name="form_type" value="reverse_text_task" />
            <input
              type="text"
              name="text"
              class="form-control mb-2"
              placeholder="Enter Text"
              required
            />
            <button class="btn btn-dark w-100" type="submit">
              Reverse
            </button>
          </form>
          <% if (reverseResult !== null) { %>
            <div class="mt-2" id="reverse-result">
              <strong>Result:</strong> <%= reverseResult %>
            </div>
          <% } %>
        </div>
      </div>

      <!-- Sentiment Analysis -->
      <div class="col-md-6 mb-4">
        <div class="task-card sentiment-task p-3">
          <div class="task-header">📊 Sentiment Analysis</div>
          <form method="POST">
            <input
              type="hidden"
              name="form_type"
              value="fake_sentiment_analysis"
            />
            <input
              type="text"
              name="text"
              class="form-control mb-2"
              placeholder="Enter Text"
              required
            />
            <button class="btn btn-primary w-100" type="submit">
              Analyze
            </button>
          </form>
          <% if (sentimentResult !== null) { %>
            <div class="mt-2" id="sentiment-result">
              <strong>Sentiment:</strong> <%= sentimentResult %>
            </div>
          <% } %>
        </div>
      </div>
    </div>

    <hr/>

 
  </div>

</body>
</html>
